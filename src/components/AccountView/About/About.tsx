import React from "react";
import { View, Text } from "react-native";

type AboutProps = {};
export const About = (props: AboutProps) => {
  return (
    <View>
      <Text>Hello</Text>
    </View>
  );
};
