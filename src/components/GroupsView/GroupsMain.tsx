import React from "react";
import { Text } from "react-native";
import { Center } from "../dumbComponents/Center";

type GroupsMainProps = {};
export const GroupsMain = (props: GroupsMainProps) => {
  return (
    <Center>
      <Text>Groups View</Text>
    </Center>
  );
};
